#!/bin/bash

# Run ESLint on staged files
git diff --cached --name-only --diff-filter=ACM | grep '\.js$' | xargs ./node_modules/.bin/eslint

# Capture exit status
ESLINT_STATUS=$?

# Exit with ESLint status
exit $ESLINT_STATUS